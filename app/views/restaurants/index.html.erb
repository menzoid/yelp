<h1>Your restaurants</h1>

  <% @restaurants.each do |restaurant| %>
    <div class="restaurant-block">
      <li class="restaurant-title"><%= link_to restaurant.name, restaurant_path(restaurant) %>
      (<%= restaurant.category %>)
      - at:  <%= restaurant.address %>
      </li>
    </div>
  <% end %>
